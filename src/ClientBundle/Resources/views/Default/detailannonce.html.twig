{% extends 'ClientBundle:Default:baseclient.html.twig' %}
{% block title %}Détails de l'annonce{% endblock %}
{% block body %}
    <h2>Détails de l'annonce</h2>

    <table class="table">
        
        <thead>
            <tr>                
                <th>Titre</th>
                <th>Image</th>
                <th>Description</th>
                <th>Prix</th>
                <th>Vendeur</th>
                <th>Dateparution</th>
                <th>Heure</th>
                <th>Telephone</th>
                <th>Categorie</th>
                <th>Localite</th>
               
            </tr>
        </thead>
        <tbody>
            <tr>
                <td>{{ annonce.titre }}</td>
                <td>
                    <div class ="imgcontainer">
                        <img class="image img-responsive" src ="{{asset('uploads/img/'~annonce.img)}}" alt="{{annonce.img}}"/>
                    </div>
                </td>
                <td class="description">{{ annonce.description }}</td>
                <td>{{ annonce.prix }} €</td>
                <td>{{ annonce.vendeur }}</td>
                <td>{{ annonce.dateparution|date("d/m/y") }}</td>
                <td>{{ annonce.dateparution|date("h:m")}}</td>
                <td>{{ annonce.telephone }}</td>
                <td>{{ annonce.categorie }}</td>
                <td>{{ annonce.localite }}</td>
            </tr>
        </tbody>
    </table>
            
    <a href="{{ path('mailform',{'mail':annonce.email}) }}">
        <button type="button" class="btn btn-default">Contact</button></a>

{% endblock %}

{% extends 'accueil.html.twig' %}

{% block body %}
    <h1>Créer une catégorie</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('listecategorie') }}">Retour à la liste</a>
        </li>
    </ul>
{% endblock %}

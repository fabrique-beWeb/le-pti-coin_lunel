{% extends 'accueil.html.twig' %}

{% block body %}
    <h2>Créer une catégorie</h2>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Créer" class="btn btn-default" />
    {{ form_end(form) }}
        <a href="{{ path('listecategorie') }}">
        <button type="button"  class="btn btn-default">Retour à la liste</button></a>

{% endblock %}

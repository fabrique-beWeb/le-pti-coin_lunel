{% extends 'accueil.html.twig' %}

{% block body %}
    <h2>Demande</h2>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ demande.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ demande.titre }}</td>
            </tr>
            <tr>
                <th>Img</th>
                <td>{{ demande.img }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ demande.description }}</td>
            </tr>
            <tr>
                <th>Estimation</th>
                <td>{{ demande.estimation }}</td>
            </tr>
            <tr>
                <th>Demandeur</th>
                <td>{{ demande.demandeur }}</td>
            </tr>
            <tr>
                <th>Dateparution</th>
                <td>{{ demande.dateparution|date("d/m/y") }}</td>
            </tr>
            <tr>
                <th>Telephone</th>
                <td>{{ demande.telephone }}</td>
            </tr>
        </tbody>
    </table>

            <a href="{{ path('mesdemandes') }}">
                <button type="button" class="btn btn-default">Retour à la liste</button></a>
 
            <a href="{{ path('editdemande', { 'id': demande.id }) }}">
                <button type="button" class="btn btn-default">Modifer</button></a>
    
            {{ form_start(delete_form) }}
                <input type="submit" value="Supprimer" class="btn btn-default">
            {{ form_end(delete_form) }}
     
{% endblock %}

{% extends 'accueil.html.twig' %}

{% block body %}
    <h2>Créer une demande</h2>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input  value="Créer" type="submit" class="btn btn-default"/>
    {{ form_end(form) }}

            <a href="{{ path('mesdemandes') }}">
                <button type="button" class="btn btn-default">Retour à la liste</button></a>
      
{% endblock %}

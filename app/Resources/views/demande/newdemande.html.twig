{% extends 'accueil.html.twig' %}

{% block body %}
    <h1>Créer une demande</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

            <a href="{{ path('listedemande') }}">
                <button type="button"  class="btn btn-default">Retour à la liste</button></a>
      
{% endblock %}
